# go-rag
Go-RAG is a simple AI document question-answering system built using Go, vector search, and local LLMs. The system ingests documents, converts them into embeddings, stores them in a vector database, retrieves relevant chunks based on user queries, and generates responses using an LLM.
